# Senus

獨立的 SEN 學生教材庫：按實際困難搵介入工具，而不是按診斷分類。本機預覽：http://localhost:3001（Cursor 雲端預覽請開 **3001** 埠）。

這不是 Mind-Note 個人筆記站。

## 本機

在倉庫根目錄：

```bash
npm run dev:senus
```

或：

```bash
cd senus
npm install
npm run dev
```

開啟 http://localhost:3001
